query ConjugationQuery($stem: String!, $honorific: Boolean!, $isAdj: Boolean!) {
  conjugation(stem: $stem, honorific: $honorific, isAdj: $isAdj) {
    name,
    conjugation,
    type,
    tense,
    speechLevel,
    honorific,
    pronunciation,
    romanization,
    reasons
  }
}